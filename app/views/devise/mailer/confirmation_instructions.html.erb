<p><%= t("devise.mailer.reset_password_instructions.greeting")%> <%= @resource.username %>!</p>

<p><%= t("devise.passwords.mailer.confirmation_instructions") %></p>

<p><%= link_to t("devise.passwords.mailer.action"), confirmation_url(@resource, confirmation_token: @token) %></p>
